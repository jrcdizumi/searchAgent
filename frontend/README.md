# 智能搜索助手 - 前端界面

这是一个使用 React + TypeScript + Vite 构建的现代化聊天界面。

## 技术栈

- ⚛️ React 18
- 📘 TypeScript
- ⚡ Vite
- 🎨 纯 CSS（无需额外 UI 库）

## 功能特性

- 💬 实时聊天界面
- 🎯 简洁美观的 UI 设计
- 📱 响应式布局（支持移动端）
- 🌈 渐变色主题
- ✨ 流畅的动画效果
- 🔄 加载状态提示
- 📝 历史消息记录
- 🧹 一键清空对话

## 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000 查看应用

### 3. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录

## 目录结构

```
frontend/
├── src/
│   ├── App.tsx          # 主应用组件
│   ├── App.css          # 样式文件
│   ├── main.tsx         # 应用入口
│   ├── index.css        # 全局样式
│   └── vite-env.d.ts    # TypeScript 声明
├── index.html           # HTML 模板
├── package.json         # 依赖配置
├── tsconfig.json        # TypeScript 配置
├── vite.config.ts       # Vite 配置
└── README.md           # 说明文档
```

## API 集成

前端通过 `/api/chat` 端点与后端通信。请确保后端服务运行在 `http://localhost:8000`。

如需修改后端地址，请编辑 `vite.config.ts` 中的 proxy 配置。

## 自定义

### 修改主题色

编辑 `src/App.css` 中的渐变色配置：

```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### 调整 API 端点

修改 `src/App.tsx` 中的 API 调用路径：

```typescript
const response = await axios.post('/api/chat', {
  message: userMessage.content,
})
```

## 开发建议

1. 使用 Chrome DevTools 进行调试
2. 推荐安装 React Developer Tools 扩展
3. 使用 ESLint 保持代码质量
4. 遵循 TypeScript 最佳实践

## 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

最低版本要求：支持 ES2020 的现代浏览器

## License

MIT

